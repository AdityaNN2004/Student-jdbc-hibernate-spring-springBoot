<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
    "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
    "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>
        
        <!-- Database connection -->
        <property name="hibernate.connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        <property name="hibernate.connection.url">jdbc:mysql://localhost:3306/StudentDB?createDatabaseIfNotExist=true&amp;useSSL=false&amp;serverTimezone=UTC</property>
        <property name="hibernate.connection.username">D5-92644-Aditya</property>
        <property name="hibernate.connection.password">manager</property> <!-- externalise -->
        
        <!-- Dialect for MySQL 8 -->
        <!--  <property name="hibernate.dialect">org.hibernate.dialect.MySQL8Dialect</property> -->
		<property name="hibernate.dialect">org.hibernate.dialect.MySQLDialect</property>

        <!-- Development console output -->
        <property name="hibernate.show_sql">true</property>
        <property name="hibernate.format_sql">true</property>
        <property name="hibernate.use_sql_comments">true</property>

        <!-- Session management -->
        <property name="hibernate.current_session_context_class">thread</property>

        <!-- Schema policy – use 'update' during dev, 'validate' in prod -->
        <property name="hibernate.hbm2ddl.auto">update</property>

        <!-- (Optional) Connection pool with HikariCP -->
        <property name="hibernate.hikari.maximumPoolSize">10</property>
        <property name="hibernate.hikari.minimumIdle">5</property>

        <!-- Entity mappings -->
        <mapping class="com.hibernate.student.entity.Student"/>
        <mapping class="com.hibernate.student.entity.Course"/>
        <mapping class="com.hibernate.student.entity.Enrollment"/>
        
    </session-factory>
</hibernate-configuration>